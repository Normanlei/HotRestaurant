class Reservation {
    constructor (name, time, size, id) {
        this.name = name;
        this.time = time;
        this.size = size;
        this.id = id;
    }

    //write to api function (print esque)
}

module.exports = Reservation;